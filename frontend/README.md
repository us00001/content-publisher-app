# Frontend (Vite + React + TypeScript)

## Setup

1. `cd frontend`
2. `npm install` (or `pnpm install`)
3. create `.env` with `VITE_API_BASE=http://localhost:3000`
4. `npm run dev`

Notes:
- Uses cookies for auth; keep `credentials: 'include'` in fetch requests.
